# Core
requests>=2.31
PyYAML>=6.0
python-dateutil>=2.8

# Data / analysis
numpy>=1.24
pandas>=2.0
scikit-learn>=1.3
hdbscan>=0.8.33

# Embeddings (primary path). If unavailable, code falls back to TF-IDF+SVD.
sentence-transformers>=2.2

# Dashboard
streamlit>=1.30
plotly>=5.18

# Tests
pytest>=7.4
