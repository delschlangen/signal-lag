# Core / deploy requirements. This is the lightweight set that runs the whole
# pipeline (and the hosted Streamlit demo) using the built-in fallbacks:
# TF-IDF+SVD embeddings and k-means clustering. For the higher-quality
# sentence-transformers + HDBSCAN path, also install requirements-full.txt.

requests>=2.31
PyYAML>=6.0
python-dateutil>=2.8

numpy>=1.24
pandas>=2.0
scikit-learn>=1.3

streamlit>=1.30
plotly>=5.18

pytest>=7.4
